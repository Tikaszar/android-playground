use playground_types::Event;
use crate::state::EditorState;

pub fn handle_file_open(state: &mut EditorState, _event: &Event) -> bool {
    // Handle file open logic
    true
}

pub fn handle_file_save(state: &mut EditorState, _event: &Event) -> bool {
    // Handle file save logic
    true
}