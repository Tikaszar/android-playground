//! Terminal implementation for Termux integration

use crate::error::{UiError, UiResult};

/// Terminal for interacting with Termux
pub struct Terminal {
    // TODO: Implement actual terminal
}

impl Terminal {
    pub fn new() -> Self {
        Self {}
    }
}